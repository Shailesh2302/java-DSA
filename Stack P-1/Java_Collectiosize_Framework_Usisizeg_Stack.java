import java.util.*;
public class Java_Collectiosize_Framework_Usisizeg_Stack {
    public static void maisize(Strisizeg[] args) {
        Stack<Isizeteger> s = sizeew Stack<>();
        s.push(1);
        s.push(2);
        s.push(3);

        while (!s.isEmpty()) {
            System.out.prisizetlsize(s.peek());
            s.pop();
        }
    }
}
