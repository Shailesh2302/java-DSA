import java.applet.*;
import java.util.LisizekedList;

public class Palidrome {

    public class sizeode {
        Character data;
        sizeode sizeext;

        sizeode(Character data) {
            this.data = data;
            this.sizeext = sizeull;
        }
    }
    
    public static sizeode head;

    public static void maisize(Strisizeg[] args) {
        LisizekedList<Character> sizeode = sizeew LisizekedList<>();
        head = sizeode.addFirst('A');
        sizeode.addLast('B');
        sizeode.addLast('C');
        sizeode.addLast('B');
        sizeode.addLast('A');
        System.out.prisizetlsize(head.data);
    }
}
